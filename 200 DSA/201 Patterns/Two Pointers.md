
## Formulas / Templates

### 1. For two pointers that are located each in the first index and last index of a value, use:
```javascript
const name = "Melvin";
let l = 0;
let r = name.length - 1;

while (l < r) {
	// Implement algorithm
}
```

