## Array
### Methods
- push() -> Adding an element to the top of the list.
- pop() -> Removing the element at the top of the list
- index()
- filter() -> Filter the list by passing a callback that specifies a Boolean condition

## Hashing
Converts data into key-value pairs

